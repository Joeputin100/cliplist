# Gradle JVM heap. The default is too small to dex the app once Firebase (Crashlytics)
# is added — D8/R8 OOM'd on CI. 3 GB is comfortable on a 7 GB GitHub-hosted runner.
org.gradle.jvmargs=-Xmx3g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8
